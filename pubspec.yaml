name: evently_task
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.2.0
  flutter_localizations:
    sdk: flutter
  intl: any
  animated_toggle_switch: ^0.8.5
  provider: ^6.1.5
  google_sign_in: ^6.1.5
  flutter_map: ^7.0.0  # Specify version 7.0.0 or later
  latlong2: ^0.9.0
  firebase_core: ^3.9.0
  firebase_auth: ^5.6.2
  cloud_firestore: ^5.6.11 # Ensure compatible latlong2 version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/svg/